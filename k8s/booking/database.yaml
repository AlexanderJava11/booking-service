apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-db

spec:
  replicas: 1

  selector:
    matchLabels:
      app: booking-db

  template:
    metadata:
      labels:
        app: booking-db

    spec:
      containers:
        - name: booking-db
          image: mysql:8.0

          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: booking-secret
                  key: db-password

            - name: MYSQL_DATABASE
              value: pensionatendb

          ports:
            - containerPort: 3306

---
apiVersion: v1
kind: Service
metadata:
  name: booking-db

spec:
  selector:
    app: booking-db

  ports:
    - port: 3306
      targetPort: 3306

  type: ClusterIP
