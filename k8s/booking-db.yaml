apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-db
  labels:
    app: booking-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-db
  template:
    metadata:
      labels:
        app: booking-db
    spec:
      containers:
        - name: booking-db
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_DATABASE
              value: "Pensionatendb"
            - name: MYSQL_ROOT_PASSWORD
              value: "bookingpassword"
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "localhost", "-pbookingpassword"]
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 10
          livenessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "localhost", "-pbookingpassword"]
            initialDelaySeconds: 30
            periodSeconds: 10
          volumeMounts:
            - name: booking-db-data
              mountPath: /var/lib/mysql
      volumes:
        - name: booking-db-data
          persistentVolumeClaim:
            claimName: booking-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booking-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: booking-db
  labels:
    app: booking-db
spec:
  selector:
    app: booking-db
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
